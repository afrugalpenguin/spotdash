# Secrets. config.json 
config.json
agent/config.json

# Per-source runtime state, such as the Spotify refresh token.
spotify_state.json
agent/spotify_state.json
spotify_art.*
agent/spotify_art.*
*_state.json

# Partition backups from tools/spot-backup.ps1: unique to one device, never commit.
spot-backup-*/

# Go build output
/agent/spotdash
/agent/spotdash.exe
/agent/dist/
*.exe
*.exe~
*.test
*.out

# Agent runtime output
*.log
agent/logs/

# Android / Gradle
shell/.gradle/
shell/build/
shell/app/build/
shell/local.properties
shell/captures/
*.apk
*.aab
*.keystore
*.jks
*.p12
keystore.properties

# Git worktrees used for isolated feature work
.worktrees/

# Editors and OS
.idea/
.vscode/
*.iml
Thumbs.db
desktop.ini

# Design mockups and scratch images, not part of the build
mockups/
artcover.png
Screenshot*.png
